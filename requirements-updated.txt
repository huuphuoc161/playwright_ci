# Updated requirements.txt for Playwright CI
# Core Playwright dependencies
playwright
pytest-playwright

# Testing framework and utilities
pytest>=7.0.0
pytest-html>=3.1.0
pytest-xdist>=3.0.0

# Configuration and utilities
PyYAML>=6.0
python-dotenv>=0.19.0

# Optional reporting (commented out problematic packages)
# allure-pytest>=2.12.0  # Uncomment if you need Allure reporting
# Note: extend-report-selenium is not a valid package name